@model IEnumerable<EducationAlarmDb.ForeignLanguage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Language)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Language)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ForeignLanguageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ForeignLanguageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ForeignLanguageId })
        </td>
    </tr>
}

</table>
